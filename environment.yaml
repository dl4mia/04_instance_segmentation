name: instance
channels:
  - defaults
  - nvidia
dependencies:
  - python=3.8
  - cudatoolkit=11.0.*
  - cudnn=8.0.*
  - jupyter
  - scikit-image
  - pip
  - pip:
    - tensorflow==2.4.*
    - csbdeep
    - stardist
